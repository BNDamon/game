namespace BlockMerge.Core
{
    /// <summary>Mirrors the 5-color palette from the HTML prototype
    /// (COLORS = ['#ff5d6c', '#4dd0e1', '#ffd34d', '#7c5cff', '#5ddc7a']).
    /// Actual RGB values are a Gameplay/view concern, not Core's.</summary>
    public enum BlockColor
    {
        Red,
        Cyan,
        Yellow,
        Purple,
        Green
    }
}
